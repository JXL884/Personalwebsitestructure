
  # Personal website structure

  This is a code bundle for website structure.
  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
